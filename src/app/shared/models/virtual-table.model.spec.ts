import { VirtualTable.Model } from './virtual-table.model';

describe('VirtualTable.Model', () => {
  it('should create an instance', () => {
    expect(new VirtualTable.Model()).toBeTruthy();
  });
});
